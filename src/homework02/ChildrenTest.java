package homework02;

// 在Children类main函数中创建Children类对象
public class ChildrenTest {
        public static void main(String[] args) {
            // 创建一个Children类对象，传入name和age参数
            Children c = new Children("光之梦", 18);
            // 调用该对象的showInfo方法
            c.showInfo();
        }
}
